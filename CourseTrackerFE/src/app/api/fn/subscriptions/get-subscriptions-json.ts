/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SubscriptionDto } from '../../models/subscription-dto';

export interface GetSubscriptions$Json$Params {
}

export function getSubscriptions$Json(http: HttpClient, rootUrl: string, params?: GetSubscriptions$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<SubscriptionDto>>> {
  const rb = new RequestBuilder(rootUrl, getSubscriptions$Json.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<SubscriptionDto>>;
    })
  );
}

getSubscriptions$Json.PATH = '/api/Subscriptions';
